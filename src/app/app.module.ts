import { NgModule } from '@angular/core';
import { BrowserModule } from '@angular/platform-browser';
import { FormsModule } from '@angular/forms'; // 必要に応じて追加

@NgModule({
    imports: [
        BrowserModule,
        FormsModule
    ],
    providers: [],
})
export class AppModule {}
